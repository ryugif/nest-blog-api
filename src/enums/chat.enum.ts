export enum ChatPriority {
    HIGH = 'HIGH',
    MEDIUM = 'MEDIUM',
    LOW = 'LOW'
}

export enum MessageType {
    text = 'text',
    media = 'media',
}

export enum SenderType {
    user = 'user',
    customer = 'customer',
    bot = 'bot',
}